import 'package:flutter/material.dart';

class ShareTodoListPage extends StatelessWidget {
  const ShareTodoListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
